apiVersion: v1
kind: Service
metadata:
  name: {{ include "ecommerce-frontend.fullname" . }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: {{ include "ecommerce-frontend.name" . }}
